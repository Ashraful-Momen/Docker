# Remove the stack (already done, but safe to retry)
docker stack rm laravel

# Leave swarm mode
docker swarm leave --force

# Restart Docker to clear stale network state
sudo systemctl restart docker

# Verify
docker network ls
